# Copy this file to .env and fill in your values
# For production, create .env.production with VITE_API_URL set to your public site
# URL (same origin recommended), e.g. https://yourdomain.org

# API base URL — local dev backend
VITE_API_URL=http://localhost:8080
